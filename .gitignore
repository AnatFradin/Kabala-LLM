# OS
.DS_Store

# Obsidian editor/workspace state
.obsidian/

# Claude Code local (machine-specific) settings
.claude/settings.local.json

# Google Drive pointer files (machine/account-specific; embed email + doc IDs,
# don't render for other users — the .md conversions are the real content)
*.gdoc
